<Window x:Class="NettoyerPc.MainForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nettoyeur PC 2000 - Menu Principal" 
        Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        Background="#F0F0F0">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- En-tête -->
        <Border Grid.Row="0" Background="#0078D7" Padding="20">
            <StackPanel>
                <TextBlock Text="NETTOYEUR PC 2000" 
                          FontSize="32" 
                          FontWeight="Bold" 
                          Foreground="White"
                          HorizontalAlignment="Center"/>
                <TextBlock Text="Système de nettoyage Windows professionnel" 
                          FontSize="14" 
                          Foreground="White"
                          HorizontalAlignment="Center"
                          Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Contenu principal -->
        <Border Grid.Row="1" Margin="40,30,40,20" 
                Background="White" 
                BorderBrush="#CCCCCC" 
                BorderThickness="1"
                CornerRadius="8">
            <StackPanel Margin="30">
                <TextBlock Text="Choisissez un mode de nettoyage :" 
                          FontSize="18" 
                          FontWeight="SemiBold"
                          Margin="0,0,0,30"/>

                <!-- Mode Complet -->
                <Border Background="#F8F8F8" 
                       BorderBrush="#0078D7" 
                       BorderThickness="2"
                       CornerRadius="6"
                       Padding="20"
                       Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="🚀 NETTOYAGE COMPLET" 
                                  FontSize="20" 
                                  FontWeight="Bold"
                                  Foreground="#0078D7"/>
                        <TextBlock Text="• Durée : 20-40 minutes" 
                                  Margin="0,10,0,0" 
                                  FontSize="13"/>
                        <TextBlock Text="• Fichiers temporaires, caches dev, navigateurs" 
                                  FontSize="13"/>
                        <TextBlock Text="• Gaming, DNS, Windows Update" 
                                  FontSize="13"/>
                        <TextBlock Text="• Scan antivirus rapide" 
                                  FontSize="13"/>
                        
                        <Button x:Name="BtnComplete" 
                               Content="DÉMARRER LE NETTOYAGE COMPLET"
                               Click="BtnComplete_Click"
                               Margin="0,15,0,0"
                               Height="45"/>
                    </StackPanel>
                </Border>

                <!-- Mode Printemps -->
                <Border Background="#FFF8F0" 
                       BorderBrush="#D13438" 
                       BorderThickness="2"
                       CornerRadius="6"
                       Padding="20">
                    <StackPanel>
                        <TextBlock Text="🔥 NETTOYAGE DE PRINTEMPS" 
                                  FontSize="20" 
                                  FontWeight="Bold"
                                  Foreground="#D13438"/>
                        <TextBlock Text="• Durée : 60-120 minutes" 
                                  Margin="0,10,0,0" 
                                  FontSize="13"/>
                        <TextBlock Text="• Tout le nettoyage complet +" 
                                  FontSize="13"/>
                        <TextBlock Text="• Point de restauration, défragmentation" 
                                  FontSize="13"/>
                        <TextBlock Text="• Scan antivirus complet, optimisations registre" 
                                  FontSize="13"/>
                        
                        <Button x:Name="BtnDeepClean" 
                               Content="DÉMARRER LE GRAND NETTOYAGE"
                               Click="BtnDeepClean_Click"
                               Style="{StaticResource DangerButton}"
                               Margin="0,15,0,0"
                               Height="45"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>

        <!-- Pied de page -->
        <Border Grid.Row="2" Background="White" BorderBrush="#CCCCCC" BorderThickness="0,1,0,0">
            <Grid Margin="20,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="✅ Droits administrateur actifs" 
                              VerticalAlignment="Center"
                              Foreground="#107C10"
                              FontWeight="SemiBold"/>
                </StackPanel>
                
                <Button Grid.Column="1" 
                       x:Name="BtnReports"
                       Content="📊 Voir les rapports"
                       Click="BtnReports_Click"
                       Padding="20,8"
                       Background="#F0F0F0"
                       Foreground="#333333"
                       Margin="10,0,0,0"/>
            </Grid>
        </Border>
    </Grid>
</Window>
